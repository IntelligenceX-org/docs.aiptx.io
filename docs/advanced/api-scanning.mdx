---
title: "API Security Testing - REST, GraphQL, gRPC Penetration Testing"
sidebarTitle: "API Scanning"
description: "Comprehensive API security testing and VAPT for REST APIs, GraphQL endpoints, and gRPC services. OpenAPI/Swagger integration, authentication testing, OWASP API Top 10 coverage, and automated vulnerability detection."
icon: "code"
og:title: "API Security Testing - REST, GraphQL, gRPC VAPT"
og:description: "Advanced API penetration testing for REST, GraphQL, and gRPC. OpenAPI integration, OWASP API Top 10 coverage, automated security scanning."
---

{/* SEO: Primary Keywords - API security testing, REST API security, GraphQL security, gRPC security, API penetration testing */}
{/* SEO: Secondary Keywords - OWASP API Top 10, OpenAPI security, API VAPT, web API security */}

# API Scanning

AIPTx provides comprehensive API security testing for modern API architectures.

## REST API Testing

### OpenAPI/Swagger Integration

```yaml
api:
  type: rest
  spec: ./openapi.yaml
  # or remote
  spec: https://api.example.com/openapi.json
```

### Custom Endpoints

```yaml
api:
  endpoints:
    - method: GET
      path: /api/users/{id}
      params:
        id: ["1", "2", "999", "admin"]

    - method: POST
      path: /api/users
      body:
        name: "{{fuzz}}"
        email: "{{fuzz}}@test.com"
        role: ["user", "admin"]
```

## GraphQL Testing

```yaml
api:
  type: graphql
  endpoint: https://api.example.com/graphql
  introspection: true

  queries:
    - |
      query {
        user(id: "{{fuzz}}") {
          email
          role
        }
      }

  mutations:
    - |
      mutation {
        updateUser(id: "{{fuzz}}", role: "admin") {
          success
        }
      }

  tests:
    - introspection_enabled
    - query_depth_limit
    - batch_query_attack
    - field_authorization
```

## gRPC Testing

```yaml
api:
  type: grpc
  endpoint: api.example.com:443
  proto_files:
    - ./protos/service.proto
  reflection: true
```

## API Security Tests

| Test | Description |
|------|-------------|
| Authentication bypass | Missing or weak auth |
| Authorization flaws | IDOR, privilege escalation |
| Input validation | Injection, overflow |
| Rate limiting | DoS protection |
| Data exposure | Sensitive data in responses |

## Response Analysis

```yaml
api:
  response_analysis:
    sensitive_data:
      - password
      - secret
      - token
      - ssn
      - credit_card
    pii_detection: true
    verbose_errors: true
```
